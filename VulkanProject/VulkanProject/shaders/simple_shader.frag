#version 450#extension GL_ARB_separate_shader_objects : enable// run for every pixel// location,in vec3 must match, not variable namelayout(location = 0) in vec3 fragColor;layout (location = 0) out vec4 outColor;void main() {    outColor = vec4(fragColor,1.0); // r,g,b,alpha}